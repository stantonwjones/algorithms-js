// a tree where a single node does not contain its data.  Instead, the data is
// represented by the path to that node (every intermediate node up to and
// including the node in question.  F/e the structure of the athena filesystem
// for organizing user accounts (s/t/a/stantonwjones)
